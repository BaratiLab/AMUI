/**
 * enums.ts
 * Enums that are commonly used between different applications (i.e. redux).
 */

export enum Status {
  Failed = "failed",
  Idle = "idle",
  Loading = "loading",
  Succeeded = "succeeded",
}
